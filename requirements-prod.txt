# Runtime dependencies for the Docker image.
# requirements.txt simply layers the test dependencies on top of this file
# (`-r requirements-prod.txt`), so the two can never drift apart.

Flask>=3.1.3
Werkzeug>=3.1.5
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Limiter==4.1.1
Flask-SocketIO==5.3.6
python-socketio==5.11.4
python-dotenv==1.0.1
email-validator==2.1.1
pywebpush==2.0.3
requests>=2.32.3
alembic==1.13.2

# Production WSGI server (async_mode 'threading' => gthread worker)
gunicorn>=21.2.0
# WebSocket transport under SocketIO's threading async mode
simple-websocket>=0.10.0
# PostgreSQL driver (manylinux + arm64 wheels, no compilation needed)
psycopg2-binary>=2.9.9